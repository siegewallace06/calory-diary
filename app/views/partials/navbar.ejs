<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand" href="/">
            <i class="fas fa-chart-pie me-2"></i>Calorie Diary
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link <%= locals.currentPage === 'dashboard' ? 'active' : '' %>" href="/">
                        <i class="fas fa-tachometer-alt me-1"></i>Dashboard
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= locals.currentPage === 'log' ? 'active' : '' %>" href="/log">
                        <i class="fas fa-plus-circle me-1"></i>Add Entry
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= locals.currentPage === 'summary' ? 'active' : '' %>" href="/summary">
                        <i class="fas fa-chart-bar me-1"></i>Summary
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= locals.currentPage === 'settings' ? 'active' : '' %>" href="/settings">
                        <i class="fas fa-cog me-1"></i>Settings
                    </a>
                </li>
            </ul>

            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                        data-bs-toggle="dropdown">
                        <i class="fas fa-ellipsis-v"></i>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><a class="dropdown-item" href="/health">
                                <i class="fas fa-heartbeat me-2"></i>Health Check
                            </a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="#" onclick="refreshData()">
                                <i class="fas fa-refresh me-2"></i>Refresh Data
                            </a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>

<script>
    // Function available to all pages
    function refreshData() {
        fetch('/api/refresh', { method: 'POST' })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    location.reload();
                } else {
                    alert('Refresh failed: ' + data.error);
                }
            })
            .catch(error => {
                alert('Refresh failed: ' + error.message);
            });
    }
</script>